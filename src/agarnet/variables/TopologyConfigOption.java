package agarnet.variables;

/* stub child class, because we want a distinct type for topology config */
public class TopologyConfigOption extends SuboptConfigOption {

  public TopologyConfigOption (String longOption, char shortOption,
                               String argDesc, String help,
                               int longoptHasArg,
                               ConfigOptionSet subopts) {
    super (longOption, shortOption, argDesc, help, longoptHasArg, subopts);
  }
}
